/**
 * @file dateTime.util.ts
 * @module frontend/src/utils
 * @description Instructions for Frontend Date/Time Formatting Utility.
 *
 * FUNCTIONS TO IMPLEMENT (no code):
 *
 * 1. formatItineraryDate(date: string | Date): string
 *    - e.g. "Wed, 15 Jan 2025"
 *
 * 2. formatTimeSlot(startTime: string, endTime: string): string
 *    - e.g. "09:00 AM – 11:00 AM"
 *
 * 3. formatDuration(minutes: number): string
 *    - e.g. 90 → "1h 30m"
 *
 * 4. daysUntil(date: string): number
 *    - Calculate days until trip start from today.
 *
 * 5. isInPast(dateTime: string): boolean
 *    - Check if a schedule slot is already past.
 *
 * KEY CONSTRAINTS:
 * - All dates stored as UTC. Display in local timezone using Intl.DateTimeFormat.
 */
